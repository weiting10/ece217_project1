#include "project1/test_search_node.h"

namespace project1 {
  SearchNode::SearchNode( const int& xArg, const int& yArg , const double& fArg, const double& gArg, const double& hArg , const std::shared_ptr< SearchNode >& bpArg ) : x(xArg), y(yArg), f(fArg), g(gArg), h(gArg), bp(bpArg) {

  


  }
}
